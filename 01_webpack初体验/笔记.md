为什么要有webpack？
因为浏览器无法识别cjs   可以通过type module 识别esm

webpack不会改变import和export之后的部门，剩下的通过babel
webpack执行 默认会找到当前目录的src/index.html进行打包
为什么有时候需要js呢？
因为esModule必须加上js

1.npx自动执行node modules下面的命令
2.script中添加命令
